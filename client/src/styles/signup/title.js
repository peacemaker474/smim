import React from 'react';
import styled from 'styled-components';

export const SignupTitle = React.memo(styled.label`
  font-size: 14px;
  font-weight: bold;
  margin: 0 0 5px 3px;
  align-self: flex-start;
`);