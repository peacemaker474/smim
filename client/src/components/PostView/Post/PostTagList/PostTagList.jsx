import React from 'react';
import PostTagListPresenter from './PostTagList.style';

export default function PostTagList({ hashtag }) {
  return (
    <PostTagListPresenter
      hashtag={hashtag}
    />
  );
}
